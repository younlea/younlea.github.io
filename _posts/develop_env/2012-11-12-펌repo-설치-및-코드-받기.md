---
title: "[펌]repo 설치 및 코드 받기."
excerpt_separator: "<!--more-->"
date: 2012-11-12 15:19:31 +0900
categories:
  - develop_env
tags:
  - develop_env
  - 개발환경

toc : true
toc_sticky : true
---

[출처] <http://endlessmadae.tistory.com/entry/repo-%EC%84%A4%EC%B9%98>

git는 소스 관리 프로그램의 일종.

repo는 Python script로  만들어진 tool.

repo 툴을 이용하여 git를 버젼별로 쉽게 받기위해 사용함.

repo 설치

$ apt-get install git-core

$ sudo apt-get install git-core gnupg

$ curl http://android.git.kernel.org/repo > ~/bin/repo

$curl https://dl-ssl.google.com/dl/googlesource/git-repo/repo > ~/bin/repo

$ chmod a+x ~/bin/repo

git 설치

$ git clone git://git.kernel.org/pub/scm/git/git.git

$ mkdir mydroid

$ cd mydroid

$ ~/bin/repo init -u git://android.git.kernel.org/platform/manifest.git

안드로이드 소스 받기

$ cd ~/mydroid

$ ~/bin/repo sync

참고해서 정리 !!

http://namhyung.springnote.com/pages/3132772

\