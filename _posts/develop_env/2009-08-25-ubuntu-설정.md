---
title: "ubuntu 설정"
excerpt_separator: "<!--more-->"
date: 2009-08-25 11:11:03 +0900
categories:
  - develop_env
tags:
  - develop_env
  - 개발환경

toc : true
toc_sticky : true
---

>>>  ubuntu 설정  <<<

\* apt-get 업데이트\
sudo apt-get update

\* Ubuntu 한글 입력기 nabi로 변경\
sudo apt-get install nabi\
sudo im-switch -c

  선택번호     대체항목\
-----------------------------------------------\
          1    /etc/X11/xinit/xinput.d/scim\
          2    /etc/X11/xinit/xinput.d/scim-immodule\
 +        3    /etc/X11/xinit/xinput.d/scim-bridge\
          4    /etc/X11/xinit/xinput.d/nabi

기본 사항[\*]을 사용하려면 엔터, 다른 것을 사용하려면 번호를 입력하십시오: 4

sudo reboot

\* minicom 설치\
sudo apt-get install minicom

\* TFTP 설정\
sudo apt-get install xinetd tftpd tftp nfs-kernel-server\
sudo vi /etc/xinetd.d/tftp  (혹은 sudo gedit /etc/xinetd.d/tftp)

sudo ifconfig eth2 192.168.100.2 up

service tftp\
{\
 protocol        = udp\
 port            = 69\
 socket\_type     = dgram\
 wait            = yes\
 user            = nobody\
 server          = /usr/sbin/in.tftpd\
 server\_args     = /tftpboot\
 disable         = no\
}

sudo mkdir /tftpboot\
sudo chmod 777 /tftpboot\
sudo /etc/init.d/xinetd restart

\* NFS 설정\
sudo vi /etc/exports (혹은  sudo gedit /etc/exports)

# /etc/exports: the access control list for filesystems which may be exported\
#               to NFS clients.  See exports(5).\
#\
# Example for NFSv2 and NFSv3:\
# /srv/homes       hostname1(rw,sync) hostname2(ro,sync)\
#\
# Example for NFSv4:\
# /srv/nfs4        gss/krb5i(rw,sync,fsid=0,crossmnt)\
# /srv/nfs4/homes  gss/krb5i(rw,sync)\
#\
/tftpboot \*(rw,no\_root\_squash,no\_all\_squash,async)

sudo exportfs -r\
sudo /etc/init.d/nfs-kernel-server restart

아래명령어를 사용하여 타겟보드(미니컴)에서  NFS 서버에 마운트해본다\
mount -t nfs 192.168.100.2:/tftpboot /mnt/nfs\
cd /mnt/nfs

\* cross compiler 환경변수 설정\
/etc/environment 파일을 아래와 같이 수정

PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/arm/arm-gcc-3.4.4/gcc-3.4.4-glibc-2.3.3/arm-linux-gnu/bin"

\*make menuconfig 오류 해결\
sudo apt-get install libncurses5-dev

\* make zImage 오류 해결(1번 권장)

(1) elayer 계정으로  부팅후 root 권한으로 설정 변경 \
$ sudo passwd root\
$ 암호 입력\
$ 암호 입력\
$ 암호 확인 입력

$ su root\
$ 암호입력

(2) root 권한으로 부팅 설정\
sudo vi /etc/passwd  (혹은  sudo gedit /etc/passwd )

elayer:x:1000:1000:elayer,,,:/home/elayer:/bin/bash 를 아래와 같이 수정한다\
elayer:x:0:0:elayer,,,:/root:/bin/bash

\* 우분투 한글팩 설치\
[http://cafe.daum.net/oddtip/Jxkw/83?docid=1CBe5|Jxkw|83|20080829200457&q=ubuntu%20%C7%D1%B1%DB&srchid=CCB1CBe5|Jxkw|83|20080829200457](http://cafe.daum.net/oddtip/Jxkw/83?docid=1CBe5%7CJxkw%7C83%7C20080829200457&q=ubuntu%20%C7%D1%B1%DB&srchid=CCB1CBe5%7CJxkw%7C83%7C20080829200457)

\* Ubuntu 한글 입력기 nabi로 변경\
~~[http://neotrinity.egloos.com/941465](http://neotrinity.egloos.com/941465)~~ *(이글루스 블로그, 서버 종료)*

\* ubuntu에서 TFTP, NFS 설치하기  \
<http://blog.daum.net/avr32/510928?srchid=BR1http%3A%2F%2Fblog.daum.net%2Favr32%2F510928>