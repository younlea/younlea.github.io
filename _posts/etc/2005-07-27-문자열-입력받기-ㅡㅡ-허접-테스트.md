---
title: "문자열 입력받기... ㅡ.ㅡ; 허접.. 테스트"
excerpt_separator: "<!--more-->"
date: 2005-07-27 17:01:43 +0900
categories:
  - etc
tags:
  - etc

toc : true
toc_sticky : true
---

#include \
#include \
void main(void)\
{\
 char str[100];\
 char problem[100]="ab cd ef gh ij kl mn";\
 //int count\_str=0;\
 //int count\_str\_star=0;\
 int same=0;\
 char \*pd;

 //scanf("%s",&str);\
 //getchar();

/\* count\_str=sizeof(str);\
 count\_str\_star=sizeof(\*str);

 printf("sizeof str = > %d \
",count\_str);\
 printf("sizeof \*str => %d",count\_str\_star);\
 \*/\
 printf("\
problem==>>%s",&problem); //문제를 출력한다. \
 printf("\
typing ==>>"); \
 fgets(str, 100 ,stdin); //문자열을 입력 받는다.\
 /\*\
 printf("%d", strlen(str)); //입력받은 문자의 갯수를 파악한다.\
 printf("\
%s",&str); //입력받은 문자를 출력한다. \
 \*/\
 pd = strstr(str,problem); // 문제와 입력한 값이 같으면 같은 부분의 주소값을 넘겨준다.\
 same =(int)( pd - str +1 );\
 /\*\
 printf("\
 pd 값 %d", (int)pd);\
 printf("\
 str 값 %d", (int)str);\
 printf("\
 같은 위치 %d\
",same);\
 \*/\
 if(same==1)printf("잘했어요~~~~");\
 else printf("잘좀쳐라... 이렇게 못치냐ㅡ.ㅡ;");

 //printf("\
typing ==>>%s",&str); //입력받은 문자를 출력한다. \
 \
 \
 \
}\