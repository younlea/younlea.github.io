---
title: "tar 쓰기..."
excerpt_separator: "<!--more-->"
date: 2011-12-14 15:46:36 +0900
categories:
  - etc
tags:
  - etc

toc : true
toc_sticky : true
---

tar를 압축을 하거나 파일을 하나로 묶을때 많이 쓴다.

tar -cvf [target name].tar [source file]   - -를 넣고 하니까 잘안된다.. ㅡ.ㅡ;

tar cvf target.tar \*    [현재 폴더 아래로 target.tar로 묶는다.]

여기서 \*는 현재 폴더안에 있는 폴더까지 포함해서 인거고

\*.\*는 그 폴더 안에 있는 파일만 묶는거다. ^^;

tar cvfz target.tar.gz \*  [현재 폴더 아래로 target.tar로 압축해서 묶는다.]

풀때

tar xvf target.tar   : 현재폴더에 묶인거 풀기

tar xvfz target.tar : 현재 폴더에 압축된거 풀기.

tar 명령에 뒤에 따라오는 옵션에 대한 설명은 아래와 같다.

- c(Create): 압축 파일 생성
- x(eXtract): 압축 파일 해제
- v(Verbose): 작업 진행상황을 화면에 출력
- z(gZip): gzip으로 압축 또는 해제
- f(File): 압축 파일 또는 archive 장치 사용(다음에 반드시 파일명 또는 장치명이 따라와야 함)

gz로 압축은..

tar czvf ...

gz를 풀때

tar xzvf